# ============================================================
# Jarvis AI Desktop Assistant — Requirements
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ============================================================

# ── GUI ─────────────────────────────────────────────────────
PySide6>=6.7.0
PySide6-Addons>=6.7.0

# ── LLM Providers ───────────────────────────────────────────
openai>=1.35.0
google-generativeai>=0.7.0
ollama>=0.2.0                   # Local models via Ollama

# ── Voice — STT ─────────────────────────────────────────────
faster-whisper>=1.0.0           # Local Whisper STT
sounddevice>=0.4.6              # Audio capture
soundfile>=0.12.1
webrtcvad>=2.0.10               # Voice activity detection
numpy>=1.26.0

# ── Voice — TTS ─────────────────────────────────────────────
edge-tts>=6.1.9                 # Microsoft Neural TTS
pyttsx3>=2.90                   # Offline TTS fallback
pygame>=2.5.2                   # Audio playback

# ── Voice — Wake Word ───────────────────────────────────────
openwakeword>=0.6.0             # Wake word detection

# ── Desktop Automation ──────────────────────────────────────
pyautogui>=0.9.54
pywin32>=306
psutil>=5.9.8
pyperclip>=1.8.2
screen-brightness-control>=0.23.0
pycaw>=20240210                 # Windows audio control

# ── Browser Automation ──────────────────────────────────────
playwright>=1.44.0
beautifulsoup4>=4.12.3
readability-lxml>=0.8.1
lxml>=5.2.2
httpx>=0.27.0

# ── Memory / Database ───────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0
aiosqlite>=0.20.0

# ── Scheduling / Notifications ──────────────────────────────
APScheduler>=3.10.4
plyer>=2.1.0                    # Cross-platform notifications
win10toast>=0.9                 # Windows toast notifications

# ── REST API ────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0.1
keyring>=25.2.1                 # Secure credential storage
rich>=13.7.1                    # Pretty console output
loguru>=0.7.2                   # Structured logging
pyyaml>=6.0.1
aiofiles>=23.2.1
watchdog>=4.0.1                 # File system watcher
requests>=2.32.3
Pillow>=10.3.0                  # Image processing
packaging>=24.1                 # Version comparison for updater
pyinstaller>=6.8.0              # For building .exe

# ── Dev / Testing ───────────────────────────────────────────
pytest>=8.2.2
pytest-asyncio>=0.23.7
pytest-qt>=4.4.0
black>=24.4.2
ruff>=0.4.8
